/**
 * Background + sprite pixel pipelines. Phase 4.
 *
 * The PPU stub keeps a placeholder framebuffer; the real pipeline writes one
 * pixel per dot during visible scanlines using the standard 16-bit shifters
 * and 8-sprite secondary OAM evaluation.
 */
export {};
